name: harmony-phase2-status-vfx-inspect-a7c4e2f1

on:
  push:
    branches:
      - refactor/harmony-main-phase2-status-vfx

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout exact branch state
        uses: actions/checkout@v4
      - name: Verify expected base identity
        run: |
          test "$(git rev-parse HEAD^)" = "32958b4c6e95d14a441ac22544f24199a6e216a6" || test "$(git rev-parse HEAD)" = "32958b4c6e95d14a441ac22544f24199a6e216a6"
      - name: Inspect Step 3 references
        run: |
          echo '--- target references ---'
          grep -nE 'show(PlayerDamage|StatusDamage|PlayerStatusSmoke|EnemyDebuffSmoke|StatusDamageQueue|PlayerHealing)|playContactHitSound|addHealingMotes|function enemyElement|const enemyElement|enemyElement[[:space:]]*=' games/harmony/main.js || true
          echo '--- imports/current extracted helpers ---'
          sed -n '1,70p' games/harmony/main.js
